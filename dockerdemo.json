{
  "builders": [{
    "type": "docker",
    "image": "debian",
    "commit": "true"
  }],
 "provisioners": [
    {
      "type": "shell",
      "inline": ["echo 'hello!' > /root/hello/file"]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "denislavdenov/docker_demo",
        "tag": "0.1"
      }
    ]
  ]
}
